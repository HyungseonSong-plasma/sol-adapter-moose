# Backend-only workspace hardening checklist

- [x] Preserve existing MOOSE process timeout and stdout/stderr isolation.
- [x] Add explicit child working-directory execution.
- [x] Persist deterministic `input.i`, `stdout.log`, and `stderr.log` artifacts.
- [x] Cover success and non-zero backend exits with unit tests.
- [ ] Exercise a serializer-generated backend-only input through a real exact-target MOOSE solve in CI.
- [ ] Preserve resulting workspace evidence as a CI artifact.
- [ ] Exact-head Rust and MOOSE CI green.

This checklist is backend-local implementation evidence only and carries no SOL thermal semantics.
